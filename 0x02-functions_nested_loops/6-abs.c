#include "main.h"
/**
 * _abs - find the absolute value of an integer
 * @n: the input to check
 * Return: absolute value of int
 */

int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	else
	{
		return (-n);
	}
}

